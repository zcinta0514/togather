# 何时何地 · 第三份实施方案：修 Bug + 首页 + 预算改造

**背景：** 第一轮功能已上线（https://heshihedi.pages.dev），但**UI 还没定稿**。
这份计划处理用户实测后发现的问题和新需求。

**执行方式：一件事一件事来，每件做完给你看，你看完再说下一件。**

---

## 当前进度

| 顺序 | 事项 | 状态 |
|---|---|---|
| 1 | **时间网格重做** | ✅ **已完成**（提交 `fc1c648`） |
| 2 | 首页落地页 | ⬜ 下一个 |
| 3 | 预算改金额 | ⬜ |
| 4 | UI 定稿迭代 | ⬜ |

**下一步就是做「二、首页 / 落地页」。** 用户已确认：落地页要放真实活动的截图（由我建示例活动、截图、脱敏）。

---

## 一、先修 Bug：时间网格重做 ✅ 已完成

**结果：320px 下格子从 13.8px 变成 32.0px，标签重叠从 16 处降到 0。**

改法：每天一列、一周一行（按天每列 1 格，按半天每列 2 格），
日期标签从「格子顶上」移到「列顶」，跨月时标出月份。

实施中还抓到一个**几何检测抓不到**的 bug：半天粒度下最后一周只有 3 天时，
CSS Grid 自动排布会把下一组的元素填进同一行的空位，「早」标签于是跑进了日期那一行。
修法是每行末尾补齐空格子 —— **只有真的把页面看一眼才会发现。**

<details>
<summary>（原诊断记录，留档）</summary>

### 实测数据（320px 屏幕 / 30 天活动）

| 指标 | 实测值 | 问题 |
|---|---|---|
| 格子尺寸 | 13.8 × 13.8 px | **远低于 44px 触控下限** |
| 折行数 | 3 行 | |
| 压在其它格子上的日期标签 | 16 个 | 折行后标签压住上一行 |
| 横向重叠的标签 | 2 对 | 标签比格子宽 |

**根因：列数写死 14 是错的。**

```ts
const columns = Math.min(slotCount, 14);   // ← 问题在这
```

14 列在 320px 上每列只剩 14px。而且折行之后，每个格子的日期标签是
绝对定位在格子上方（`-top-4`）的，第二行开始就正好压在上一行的格子上。

**格子本身就是 button，所以用户说的「按键重叠」和「日期重叠」是同一件事。**

### 改法：按「天」分列，一周一行

不再把所有格子平铺成 14 列，改成**每天占一列**：

- **按天粒度**：每列 1 个格子，7 列 = 一周
- **按半天粒度**：每列 2 个格子（上=上午，下=下午），仍然 7 列 = 一周
- 日期标签放在**列顶**（不是格子顶），折行时不会压到上一行

这样：
- 320px 上每列约 36px，格子可点
- 折行不再有标签重叠
- 「一周一行」也符合人对日程的直觉

还要加的：
- **格子最小尺寸兜底**（`min-height`），避免极端情况下又变得太小
- **月份分隔**：跨月时在行首标出「10 月」「11 月」，不然 30 天以上找不到北

### 验收标准

在 320px / 390px 两个宽度下，用几何检测确认：
- 0 个标签压在其它格子上
- 0 对横向重叠的标签
- 每个格子 ≥ 32px
- 无横向溢出

</details>

---

## 二、首页 / 落地页

**现状：** 访问 `/` 直接跳到 `/new`（创建活动表单）。第一次来的人不知道这是干什么的。

**改成：** `/` 是一个落地页，`/new` 保持是创建表单。

### 落地页要有

1. **一句话说清是什么**
   「什么时候有空，想去哪里 —— 一起定」
2. **三步说明**（配小图）
   - 建个活动，把链接发群里
   - 大家点开涂自己的空闲时间、选想去的地方
   - 系统算出大家都能去的方案
3. **为什么不一样**（这是区别于 Doodle 的地方）
   用设计文档 §3.1 那个反例做成一张对照图：
   「去云南要 5 天、去露营只要 2 天 —— 时间和地点不能分开算」
4. **两个入口**
   - 主按钮：发起一个活动 → `/new`
   - 次入口：朋友发我链接了 → 一个输入框，粘贴链接直接跳过去
5. **分享到群里的那一段文案**（方便第一次用的人直接复制）

### 顺带

- `index.html` 的 OG 标签已有，落地页继承即可
- 底部放一句「免费、不用注册、不收集手机号」

---

## 三、预算改成具体金额

**现状：** 提名时选「省一点 / 一般 / 舍得花」三档，只能展示，没法算。

**改成：** 每个人对每个目的地**填一个整数金额**（元），然后算统计。

### 数据模型

`votes` 表加一列 `budget_amount INTEGER`（可空）：

| 字段 | 含义 |
|---|---|
| `level` | 想去(2) / 都行(1) / 不想去(0) —— 不变 |
| `budget_amount` | **我最多愿意为这趟花多少**（元，整数，选填） |

语义：绑在「我对这个目的地的态度」上，而不是单独一张表 ——
「我想去云南，愿意花 3000」是一句话，拆开没意义。

**选填**，不强制。强制填会让人卡住，而且填不准的数字比如实不填更糟。

### 展示

每个目的地显示：
- **中位数**（主）+ **平均**（辅）
- 范围：最低 ~ 最高
- 填了的人数：`3/6 人填了预算`
- 如果填的人太少（< 半数），标注「样本太少，参考意义有限」

**为什么中位数放主位**：一个人填 50000 就能把平均值拉飞，中位数不会。
这种「有人乱填」的场景下中位数更可靠。

### 界面上要讲清楚

「愿意花多少」和「这趟要花多少」是两回事。界面上要明确写：
**「你最多愿意为这趟花多少？」** —— 不是问目的地真实价格。

### 方案卡片上带出来

结果页的每个方案卡片，如果该目的地有预算数据，附一行：
`人均约 2000（中位数，5 人填）`

---

## 四、UI 定稿流程

**当前状态：材质和动效已经落地，但还没定稿。**

用户明确说了「后面还要调整，还没定稿」。所以建立这个循环：

1. 我改一版 → 部署到线上 + 给你几张关键页面的截图
2. 你看完指出要改的
3. 一次改一到两处，不堆在一起改 —— 堆着改完了你分不清是哪处带来的变化
4. 你说「这版行」才算定稿

**这一轮排在最后**，等前三个做完再开始，因为网格重做本身就会改变界面的观感。

---

## 执行顺序

| 顺序 | 做什么 | 为什么排这 |
|---|---|---|
| 1 | 时间网格重做 | 这是**功能性缺陷**，格子小到点不准，比什么都优先 |
| 2 | 首页落地页 | 别人第一次拿到链接会先看这个，影响「第一次用的人懂不懂」 |
| 3 | 预算改金额 | 需要动数据库（加列 + 迁移），排在 UI 定稿前，免得多改一遍界面 |
| 4 | UI 定稿迭代 | 前三项都会改变界面，放最后一起收 |

---

## 待确认

1. ~~**「按键重叠」**~~ —— 已确认就是时间网格那个问题（格子本身是 button），已修。
   如果之后在别的页面又看到，再单独提。

2. ~~**预算的金额问的是什么**~~ —— **已确认：每个人对每个目的地填「我最多愿意为这趟花多少（人均）」。**
   算出来的是意愿分布，中位数放主位、平均数放辅位。
   界面上必须写清楚是在问个人意愿，不是问目的地真实价格。

3. ~~**落地页要不要放示例**~~ —— **已确认：要，放真实活动截图。**
   由我建一个假数据的示例活动、截图、脱敏后用。用假名字假地名，不涉及真实信息。
